---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: nextcloud-config-v1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn