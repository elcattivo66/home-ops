---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: calibre-web
  namespace: default
spec:
  chart:
    spec:
      chart: app-template
      version: 1.1.3
      interval: 15m
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  interval: 15m
  values:
    image:
      repository: ghcr.io/linuxserver/calibre-web
      tag: 0.6.19-ls178@sha256:9a21e834059de2f0b730e6368e21119c6fc720f0d912a8522a96f6e041118444

    env:
      DOCKER_MODS: ghcr.io/linuxserver/mods:universal-calibre
      PGID: "1000"
      PUID: "1000"

    service:
      main:
        ports:
          http:
            port: 8083

    persistence:
      config:
        enabled: true
        existingClaim: calibre-web-config-v1
      books: 
        enabled: true
        type: custom
        volumeSpec:
          nfs:
            server: ${NFS_ADDR}
            path: ${MEDIADIR}/ebooks/calibre_library
        mountPath: /books
        readOnly: false

    resources:
      requests:
        cpu: 15m
        memory: 249M
      limits:
        memory: 299M

    probes:
        startup:
          spec:
            initialDelaySeconds: 60
            failureThreshold: 160