---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: media
  name: local-path-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
