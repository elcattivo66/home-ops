---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: nextcloud-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn