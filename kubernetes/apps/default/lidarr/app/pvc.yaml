---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: lidarr-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
