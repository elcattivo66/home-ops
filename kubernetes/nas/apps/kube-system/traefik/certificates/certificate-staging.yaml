apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nas-domain
  namespace: kube-system
spec:
  secretName: "nas-domain-staging-tls"
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - "${SECRET_NAS_DOMAIN}"
    - "*.${SECRET_NAS_DOMAIN}"
