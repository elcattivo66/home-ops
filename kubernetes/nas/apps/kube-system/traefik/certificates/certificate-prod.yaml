apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nas-domain
  namespace: kube-system
spec:
  secretName: "nas-domain-prod-tls"
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - "${SECRET_NAS_DOMAIN}"
    - "*.${SECRET_NAS_DOMAIN}"
