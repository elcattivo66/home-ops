---
apiVersion: v1
kind: Service
metadata:
  name: cloudnative-pg
  namespace: databases
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  externalIPs: ["${SVC_POSTGRES_ADDR}"]
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    postgresql: cloudnative-pg
    role: primary
