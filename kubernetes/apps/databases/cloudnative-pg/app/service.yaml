---
apiVersion: v1
kind: Service
metadata:
  name: cloudnative-pg
  namespace: databases
spec:
  type: LoadBalancer
  annotations:
    coredns.io/hostname: postgres.${SECRET_DOMAIN}
  externalTrafficPolicy: Local
  loadBalancerIP: ${SVC_POSTGRES_ADDR} #["${SVC_POSTGRES_ADDR}"]
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    postgresql: cloudnative-pg
    role: primary
