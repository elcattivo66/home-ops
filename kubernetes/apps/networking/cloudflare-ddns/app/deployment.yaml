---
kind: Deployment
apiVersion: apps/v1
metadata:
  namespace: networking
  name: cloudflare-ddns
  labels:
    app: cloudflare-ddns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudflare-ddns
  template:
    metadata:
      labels:
        app: cloudflare-ddns
    spec:
      # values-ish: https://hotio.dev/containers/cloudflareddns/
      containers:
        - name: cloudflare-ddns
          image: hotio/cloudflareddns:release-20220813050914
          env:
            - name: TZ
              value: ${TZ}
            - name: CF_HOSTS
              value: ${SECRET_DOMAIN}
            - name: CF_ZONES
              value: ${SECRET_DOMAIN}
            - name: CF_USER
              valueFrom:
                secretKeyRef:
                  name: cloudflare-ddns
                  key: CLOUDFLARE_EMAIL
            - name: CF_APIKEY
              valueFrom:
                secretKeyRef:
                  name: cloudflare-ddns
                  key: CLOUDFLARE_APIKEY
            - name: CF_RECORDTYPES
              valueFrom:
                secretKeyRef:
                  name: cloudflare-ddns
                  key: CF_RECORDTYPES
            # https://github.com/docker-hotio/docker-cloudflare-ddns#log-levels
            - name: LOG_LEVEL
              value: "2"
            - name: INTERVAL
              value: "600"
          resources:
            requests:
              cpu: 20m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
